import React from 'react';

const PackagesList: React.FC = () => {
  return <div>
      <form>
          <input />
          <button>Search</button>
      </form>
  </div>;
};

export default PackagesList;
